import clsx from 'clsx'
import style from './index.module.scss'

const Goal = () => {
  return (
    <div className={clsx(style.goal)}>
        <div className={clsx(style.goalPad)}>
            
        </div>

    </div>
  )
}

export default Goal